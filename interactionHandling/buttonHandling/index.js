const { handleDeleteButton } = require('./deleteButton');
const { handleSendButton } = require('./sendButton');
const { handleListButton } = require('./listButton')

module.exports = {
  handleDeleteButton,
  handleSendButton,
  handleListButton
};