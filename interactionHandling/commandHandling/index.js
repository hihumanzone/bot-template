const { handleCommand1 } = require('./command1');
const { handleUserInfoCom } = require('./userInfo');
const { handleMsgInfoCom } = require('./msgInfo');

module.exports = {
  handleCommand1,
  handleUserInfoCom,
  handleMsgInfoCom
};